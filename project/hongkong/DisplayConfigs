!
! Auto display configurations
!
! $Id: DisplayConfigs,v 1.3 1995-04-29 23:20:55 granger Exp $

!
! The dconfig dir and dconfig pd dir settings were moved to dm.config
! so that they precede reading the IconBar
!

!
! The ubiquitous whiteclock pd
!
beginpd whiteclock
component clock
	parameter background white
	parameter foreground black
	parameter xfont '-*-new century schoolbook-bold-r-*-*-25-*-*-*-*-*-*-*'
endpd


! The only configs here are the empty one, the linked zoom one, and none.
! The empty one is here as a last resort, in case the one in config
! directory is deleted by the user.  The zoom is here because links are not
! handled correctly in saved config files.

config empty
	widget 'iconbar' 6 27 68 471
endconfig

! Used to restore a config by first changing to this one
!
config none
endconfig

!
! Generic linked PD to display one window zoomed.
!
config zoom 1
	window 'humungo' 5 5 1100 850 gp gp.init
		buttonmap 'big'
		linkpd 1
	endwindow
endconfig
