!
! Check project dirs first for maps and icons.  The restricted directories
! contain files which override the usual defaults when limiting the UI.
!
set iconpath concat3("./restricted/icons,./icons,", c$libdir, "/icons")
set mappath concat("./maps,", c$libdir)
set RequirePath concat('./restricted/modules,./modules,',RequirePath)

require global
require SiteChange


